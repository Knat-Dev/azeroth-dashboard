# Database (AC's MySQL - already exposed on host)
DB_HOST=host.docker.internal
DB_PORT=3306
DB_ROOT_PASSWORD=password

# SOAP (AC's worldserver - already exposed on host)
SOAP_HOST=host.docker.internal
SOAP_PORT=7878
SOAP_USER=admin
SOAP_PASSWORD=admin

# Dashboard
JWT_SECRET=change-me-to-something-random
DASHBOARD_PORT=3000
API_PORT=3001

# Logs (path to AC server log directory on host)
AC_LOGS_DIR=../env/dist/logs

# Backups
BACKUP_RETENTION_DAYS=30
